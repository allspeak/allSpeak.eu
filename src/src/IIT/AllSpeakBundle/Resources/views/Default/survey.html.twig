{% extends 'base.html.twig' %}

{% block body %}
    <div class="row survey">
        <div class="col-sm-offset-1 col-sm-10">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
